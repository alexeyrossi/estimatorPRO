import React from 'react';

export const InputLabel = ({ label }: { label: string }) => (
    <label className="ml-1 mb-1.5 block text-[10px] font-semibold uppercase tracking-widest text-slate-500">{label}</label>
);
